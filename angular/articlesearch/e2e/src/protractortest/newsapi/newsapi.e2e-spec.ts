/* import { NewsapiPage } from "./newsapi.po";
import { ProtractorExpectedConditions, protractor } from 'protractor';

describe('Newsapi Page', ()=>{
    let page : NewsapiPage;

    const EC = protractor.ExpectedConditions;

    beforeEach(()=>{
        page = new NewsapiPage();
    });

    it('should be call the favourite articles page', ()=>{
        page.getFavouriteArticleButton().click();
      //  browser.wait(EC.visibilityOf(news.getTitle()));
    })
}) */